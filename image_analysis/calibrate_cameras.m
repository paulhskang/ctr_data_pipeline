%% Calibrate cameras

%% App to calibrate cameras
stereoCameraCalibrator;

%% Save the calibrator script and calibration session
save('/path/to/save/stereoParams.mat', 'stereoParams');   % to save calibration variable in workspace